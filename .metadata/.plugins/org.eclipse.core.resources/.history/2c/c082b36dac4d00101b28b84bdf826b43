package launcherClassObjects;

import java.util.Random;
import javafx.scene.image.Image;
import javafx.scene.image.ImageView;

public class RandomCats {
	
	private Random randomCat = new Random();
	
	//This is the range/ total amount of pictures the program can choose from, update later
	final int MIN_VAL = 1;
	final int MAX_VAL = 5;

	
	private Random xAxis = new Random();
	private Random yAxis = new Random();
	
	private Image catImage;
	ImageView catImgView;
//	private String imageLocation = "\\curiouscats\\src\\main\\resources\\catLibrary\\1.png\"";
	
	
	//No argument constructor, shouldn't need more
	public RandomCats() {
//		catImage = new Image(imageLocation);
//		catImgView = new ImageView(catImage);
		
		Image catImage = new Image(getClass().getResource("/catLibrary/1.png").toExternalForm());
		catImgView = new ImageView(catImage);
	}
	
	
	public Image getKat() {
		
		return catImage;
	}
	
	public ImageView getCat () {
		return catImgView;
	}
	public String getString() {
		return null;
//		return imageLocation;
	}
}
